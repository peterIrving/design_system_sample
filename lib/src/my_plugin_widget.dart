import 'package:flutter/widgets.dart';

class MyPluginWidget extends StatelessWidget {
  const MyPluginWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("my plugin widget!");
  }
}
